// Entorno "production" genérico (usado por `ng build` default / --configuration=production).
// Por compatibilidad, apunta al cliente principal (pfparkapp).
// Para deploys reales usar --configuration=pfparkapp o --configuration=rivera-cba explícitamente.

export { environment } from './environment.pfparkapp';
